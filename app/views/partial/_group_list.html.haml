- current_user.groups.each do |group|
  = link_to group_messages_path(group), class: "chat-nav__group__list", id: "group__#{group.id}" do
    .chat-nav__group__list__group-name
      = group.name
    .chat-nav__group__list__new-message
      = group.last_message
